import { MimeTypes } from '../enum/file';

export const SUPPORTED_PICTURE_TYPES = [MimeTypes.JPEG, MimeTypes.JPG, MimeTypes.PNG];
export const FILE_MAX_SIZE = 2097152; // 2mb
export const FILE_MIN_SIZE = 0; // 0kb
